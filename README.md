SmartFile is a web-based file processing application built using Flask (Python) that allows users to upload, convert, merge, compress, and download files from a single, easy-to-use interface.
The application is designed to work on all devices (mobile, tablet, desktop) with a responsive UI.
